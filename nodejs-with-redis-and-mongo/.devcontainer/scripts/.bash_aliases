# This is to setup the aliases for your DevContainer. This can be configured in whatever way is best for you.
# Many of the Framework generators like create-react-app are set to use npx so that it always generates the latest version.
# The Dockerfile will copy & move this to the proper place where bash/zsh can utilize these for the environment.

alias cra="npx --no-install -q create-react-app"
alias create-react-app="npx --no-install -q create-react-app"
alias create-nx-workspace="npx --no-install -q create-nx-workspace"
alias depcheck="npx --no-install -q depcheck"
alias l="ls -CF"
alias la="ls -A"
alias lerna="npx --no-install -q lerna"
alias ll="ls -la"
alias reset="~/.bashrc"
alias redis="redis-cli"
