# The post-build-env files are used to setup environment variables that are to be made accessible after the build process.
# These parameters are to configure the Redis Docker image.

# REDIS_PASSWORD="your-password-here"